# /prd-go — Write a Production-Ready PRD

Usage: `/prd-go [feature or topic]`

**Arguments:** $ARGUMENTS

---

## Purpose

Write production-ready PRDs for 97 Display's 3-pillar platform rebuild. Follows JW's 16-section template with F-numbered requirements, testable acceptance criteria, and proper data model specs. Output stored in `docs/prds/` with timestamped filenames.

---

## Instructions for Claude

Read the full skill definition from `/Users/msichris/.claude/skills/prd-go/SKILL.md` and follow it exactly.

The feature to write a PRD for is: $ARGUMENTS

If no arguments provided, ask the user what feature should this PRD cover.
